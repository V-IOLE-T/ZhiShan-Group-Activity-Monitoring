# 项目进程记录（2026-02-25）

## 本次目标

- 修复“公告沉淀到多维表格”报错，确保公告消息可稳定写入。

## 已完成

- 将公告归档写入字段收敛为 5 项：`消息ID`、`话题ID`、`发送者姓名`、`消息内容`、`发送时间`。
- 公告链路改为仅提取文本内容，不再携带附件/人员/链接等高风险字段。
- `发送时间` 固定为文本格式（`YYYY-MM-DD HH:mm:ss`）。
- 通过只读 API 核对 Bitable 表结构，确认公告表字段为上述 5 列。
- 修正 `config/.env` 中 `ARCHIVE_TABLE_ID`：由错误的 app token 改为公告表 `table_id`（`tbluetYHwUas3ayf`）。
- 验证 `records/search` 接口返回 `code=0`，线上日志已从报错转为正常。

## 关键结论

- 本次核心故障不在字段复杂度本身，而在配置层：`ARCHIVE_TABLE_ID` 使用了错误标识。
- 在飞书 Bitable 场景下，排障优先级应为：`table_id` 正确性 > 字段名匹配 > 字段类型匹配。

## 当前状态

- 公告沉淀：已恢复正常（用户确认“现在不报错了”）。
- 其他链路：本次未改动（活跃度统计、Pin、月度归档逻辑保持原状）。

