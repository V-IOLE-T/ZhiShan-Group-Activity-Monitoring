# 【PM 专用改需求模板｜实时事件系统】

你是 Codex，请直接在仓库内实现改动并执行验证，不要只给建议。
先读取相关代码和测试，再做最小改动，最后给出变更清单与测试结果。

## 0. 任务背景

- 项目：<项目名>
- 场景：<一句话描述业务场景>
- 目标文件（已知）：`<file1>`, `<file2>`
- 参考文档（可选）：<doc/path>

## 1. 业务规则区（必须遵守）

1. <规则1：必须行为，写成可验证句子>
2. <规则2：必须行为，写成可验证句子>
3. <规则3：异常场景下的行为>
4. 优先级：若冲突，按 <规则顺序> 执行。

### 1.1 边界与异常

1. 输入为空时：<期望行为>
2. 外部依赖失败时：<期望行为>
3. 幂等/去重要求：<期望行为>

## 2. 禁止改动区（硬约束）

1. 禁止修改文件：`<fileA>`, `<fileB>`
2. 禁止改变行为：<行为A，如统计口径/鉴权方式/接口协议>
3. 禁止引入：<新依赖/重构范围/数据库结构变更>
4. 非目标：<本次不做什么>

## 3. 实施范围区（本次允许改动）

1. 可改文件：`<file1>`, `<file2>`, <test_file>
2. 可新增内容：<小型工具函数/测试用例/日志文案>
3. 改动策略：最小改动，不做无关重构。

## 4. 验收区（Definition of Done）

1. 功能验收：
   - [ ] <场景A> 输入 `<X>`，输出 `<Y>`
   - [ ] <场景B> 输入 `<X>`，输出 `<Y>`
   - [ ] <场景C> 异常时仍满足 `<Y>`
2. 代码验收：
   - [ ] 仅修改允许范围内文件
   - [ ] 关键路径有测试覆盖
   - [ ] 日志语义与规则一致
3. 输出验收：
   - [ ] 给出修改文件列表
   - [ ] 给出关键代码位置（文件+行号）
   - [ ] 给出测试命令和结果摘要

## 5. 回归区（必须执行）

1. 定向测试：
   - `python -m unittest <target_test_module> -v`
2. 相关回归（非全量）：
   - `python -m unittest <related_test_module1> <related_test_module2> -v`
3. 若测试失败：
   - 先修复本次引入问题，再重跑；
   - 无法修复时明确阻塞点和影响范围。

## 6. 交付格式区（回复模板）

1. 先给“结果结论”（是否完成、是否通过验收）。
2. 再给“改动摘要”（按文件列出）。
3. 再给“验收与回归结果”（命令 + 通过/失败 + 关键信息）。
4. 最后给“风险与后续建议”（仅列必要项）。
